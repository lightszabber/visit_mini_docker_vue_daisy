@"
# visit_mini_docker_vue_daisy

Docker mini projekt:

- API: Node + Express + PostgreSQL (visits számláló)
- Frontend: Vue 3 + Vite + Vue Router + Pinia
- UI: Tailwind CSS + daisyUI
- Adminer: DB admin felület

## Indítás

```bash
docker compose up --build
